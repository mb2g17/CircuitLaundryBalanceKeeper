// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.mattbarnes.circuitlaundrybalancekeeper"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
